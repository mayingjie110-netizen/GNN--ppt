# 图神经网络入门与大图训练内存瓶颈讲解稿

## 第 1 页：图神经网络是什么

大家好，今天我们讲图神经网络，也就是 Graph Neural Networks，简称 GNN。

我们平时熟悉的神经网络，通常处理的是表格、图片、文本这样的数据。图片可以看成规则的二维网格，文本可以看成一维序列，而图数据的特点是：数据之间有复杂的连接关系。比如社交网络里人与人之间的关注关系，分子里原子之间的化学键，论文之间的引用关系，这些都天然是图。

这页右边的交互图展示了 GNN 最重要的直觉：一个节点不是孤立地做预测，而是会从它周围的邻居那里接收信息。经过一层 GNN，它看到一跳邻居；经过两层，它看到两跳邻居；层数越深，信息传播得越远。

所以我们可以先记住一句话：GNN 的本质，是让神经网络沿着图的边传播信息，并学习如何聚合这些信息。

## 第 2 页：神经网络简介

在进入 GNN 之前，我们先快速回顾普通神经网络。

普通神经网络可以理解为一串可微函数。输入向量进入第一层，经过线性变换，再经过非线性激活，得到隐藏表示；这个过程一层一层继续，最后输出预测结果。

这里的公式

\[
h^{(l)}=\sigma(W^{(l)}h^{(l-1)}+b^{(l)})
\]

表示第 \(l\) 层的隐藏表示，是上一层表示乘以权重矩阵、加上偏置，再经过激活函数得到的。

训练时，我们用损失函数衡量预测和真实标签之间的差距。然后通过反向传播计算梯度，用梯度下降更新参数：

\[
\theta \leftarrow \theta-\eta\nabla_\theta\mathcal{L}
\]

这里 \(\theta\) 是模型参数，\(\eta\) 是学习率，\(\mathcal{L}\) 是损失函数。

普通神经网络的问题在于，它通常假设输入已经是一个固定大小、规则排列的张量。但图数据并不满足这个假设：节点数量不固定，邻居数量不固定，节点顺序也没有天然意义。这就是我们需要 GNN 的原因。

## 第 3 页：为什么图很重要

图是一种非常通用的数据表示方式。它关注的不是单个对象，而是对象之间的关系。

比如社交网络中，人可以作为节点，好友关系或关注关系作为边；分子中，原子是节点，化学键是边；引用网络中，论文是节点，引用关系是边；交通网络中，路口是节点，道路是边。

甚至图像和文本也可以用图来理解。图像中的像素可以看成节点，相邻像素之间有边；文本中的词可以看成节点，词与词之间按照顺序或语义关系连接。

这说明图不是某个小众领域的数据结构，而是一种描述“实体和关系”的通用语言。

在图里，我们通常关心三类信息：节点，也就是实体本身；边，也就是实体之间的关系；还有全局信息，比如整张图的类别、环境或上下文。

## 第 4 页：图像作为图

这一页展示图像如何被看成图。

通常我们认为图片是一个二维矩阵，每个位置是一个像素，每个像素有 RGB 值。但如果从图的角度看，每个像素都可以作为一个节点，相邻像素之间连边。

大家可以点击图中的像素，观察左边像素值变化后，右边的图结构表示和邻接矩阵如何同步变化。

这个例子说明，图其实可以覆盖很多熟悉的数据类型。只不过图像这种数据结构非常规则，所以我们通常使用 CNN；而真实世界里的很多图，比如社交网络、分子图、知识图谱，并没有这么规则，这时 GNN 就更有优势。

## 第 5 页：传统神经网络在图上的局限

为什么不能直接把图塞进普通神经网络？

第一个问题是图的大小不固定。不同图可能有不同数量的节点和边，很难像图片一样统一成固定尺寸。

第二个问题是邻居数量不固定。图片里每个像素周围的邻居数量基本固定，但图里有的节点只有一两个邻居，有的节点可能有成千上万个邻居。

第三个问题是节点顺序没有意义。同一张图，我们给节点换一套编号，邻接矩阵就变了，但图本身并没有变。模型应该对这种编号变化保持稳定，也就是具备排列不变性或排列等变性。

第四个问题是稀疏性。真实图通常非常稀疏，如果用完整邻接矩阵存储，需要 \(O(|V|^2)\) 的空间，但实际存在的边可能远远少于这个数量。

所以处理图数据的模型，必须尊重图结构本身，而不是依赖固定顺序和固定网格。

## 第 6 页：图的数学格式

现在我们正式定义图。

一张图可以写成：

\[
G=(V,E,u)
\]

其中 \(V\) 是节点集合，\(E\) 是边集合，\(u\) 是全局属性。

如果每个节点都有一个特征向量，我们可以把所有节点特征堆成矩阵：

\[
X\in\mathbb{R}^{|V|\times d_v}
\]

这里 \(|V|\) 是节点数，\(d_v\) 是每个节点特征的维度。

边也可以有特征，比如分子图中不同类型的化学键。边特征可以写成：

\[
Z\in\mathbb{R}^{|E|\times d_e}
\]

而边的连接关系通常用边列表表示：

\[
E=\{(s_k,t_k)\}_{k=1}^{|E|}
\]

也就是说，第 \(k\) 条边从源节点 \(s_k\) 指向目标节点 \(t_k\)。

这页的交互图可以帮助大家理解：节点、边和全局属性都可以被转化为张量，而连接关系告诉模型信息应该沿着哪些路径流动。

## 第 7 页：邻接矩阵与邻接表

图的连接关系有两种常见表示方式：邻接矩阵和邻接表。

邻接矩阵 \(A\) 是一个 \(|V|\times |V|\) 的矩阵。如果节点 \(i\) 和节点 \(j\) 之间有边，那么 \(A_{ij}=1\)，否则为 0。

邻接矩阵直观，但对大图来说非常浪费。因为很多真实图都是稀疏的，大部分节点对之间没有边。

邻接表只存真实存在的边，比如：

\[
[(s_1,t_1),(s_2,t_2),\dots,(s_{|E|},t_{|E|})]
\]

它的空间复杂度是 \(O(|E|)\)，而不是 \(O(|V|^2)\)。

实际 GNN 框架，比如 PyTorch Geometric 或 DGL，通常都围绕边列表、稀疏矩阵、gather 和 scatter 操作来实现消息传递。

## 第 8 页：最简单的 GNN

最简单的 GNN 可以理解为 graph-in graph-out：输入是一张图，输出还是一张图。

它会更新节点、边和全局属性的表示，但不改变图的连接结构。

比如每个节点的向量 \(h_v\) 可以经过一个 MLP 变成新的节点向量：

\[
h_v'=\phi_V(h_v)
\]

边特征和全局特征也可以分别更新：

\[
e_{uv}'=\phi_E(e_{uv}),\quad u'=\phi_U(u)
\]

不过这个最简单版本还没有真正利用图的连接关系。它只是对每个节点、每条边、全局属性分别做变换。真正让 GNN 变强的，是下一步：把邻居的信息传过来。

## 第 9 页：Pooling 信息聚合

Pooling 的作用，是把一组信息聚合成一个表示。

比如我们想预测某个节点，但这个节点本身没有足够信息，而和它相连的边包含重要信息。这时就可以把相关边的信息聚合到节点上。

公式可以写成：

\[
m_v=\rho(\{h_e:e\rightarrow v\})
\]

这里 \(m_v\) 表示聚合到节点 \(v\) 的信息，\(\rho\) 是聚合函数，比如 sum、mean 或 max。

为什么聚合函数很重要？因为节点的邻居没有固定顺序。如果我们把邻居顺序打乱，模型输出不应该改变。因此聚合函数通常要满足排列不变性。

这页的交互图中，hover 一个目标节点，可以看到哪些边会被收集起来参与聚合。

## 第 10 页：Message Passing 核心迭代

现在进入 GNN 最核心的部分：message passing，也就是消息传递。

一次消息传递通常分三步。

第一步，为每条边生成消息：

\[
m_{uv}^{(k)}=\phi_m(h_u^{(k)},h_v^{(k)},e_{uv})
\]

这表示从邻居节点 \(u\) 到目标节点 \(v\) 的消息，可以由源节点表示、目标节点表示和边特征共同决定。

第二步，把目标节点收到的所有消息聚合：

\[
\bar m_v^{(k)}=\rho(\{m_{uv}^{(k)}:u\in\mathcal{N}(v)\})
\]

这里 \(\mathcal{N}(v)\) 是节点 \(v\) 的邻居集合。

第三步，用聚合后的消息更新节点表示：

\[
h_v^{(k+1)}=\phi_u(h_v^{(k)},\bar m_v^{(k)})
\]

这三个步骤反复堆叠，就形成了多层 GNN。每多一层，节点就能看到更远一跳的结构信息。

## 第 11 页：GCN 的矩阵形式

GCN，也就是 Graph Convolutional Network，可以看成 message passing 的一种经典形式。

它的更新公式是：

\[
H^{(k+1)}=\sigma(\hat D^{-1/2}\hat A\hat D^{-1/2}H^{(k)}W^{(k)})
\]

这里 \(H^{(k)}\) 是第 \(k\) 层所有节点的表示矩阵。

\(\hat A=A+I\)，表示在原来的邻接矩阵上加自环。加自环的意思是：节点在聚合邻居信息时，也保留自己的信息。

\(\hat D\) 是度矩阵，用来做归一化。如果不归一化，高度节点的影响会过大。

这个公式可以理解为：先用邻接矩阵把邻居表示聚合起来，再乘以可学习参数 \(W\)，最后经过激活函数。

GCN 的一个重要特点是，\(K\) 层 GCN 最多只能传播 \(K\)-hop 的信息。这一点后面会和大图训练的内存瓶颈联系起来。

## 第 12 页：GNN Playground

这一页是一个交互式 Playground，用分子图任务展示 GNN 的实际效果。

在这个任务中，每个分子是一张图。原子是节点，化学键是边。模型要预测这个分子是否具有某种气味特征。

大家可以观察几个模型设置：GNN 的层数、聚合函数、节点 embedding 维度、边 embedding 维度、全局 embedding 维度等。

这个例子想说明的是：GNN 不是只有一种固定架构，而是一类模型。我们可以选择不同的消息传递方式、聚合方式和表示维度。

同时也可以看到，更多参数并不总是绝对更好。图任务的效果常常和图结构、任务目标、采样方式、聚合函数都有关系。

## 第 13 页：梯度传递的内存局限

前面我们说，多层 GNN 可以让节点看到更远的邻居。但这也带来一个严重问题：大图训练时，邻居会快速膨胀。

假设我们从一个 mini-batch 的节点 \(B\) 出发。第一层需要这些节点的一跳邻居，第二层需要两跳邻居，第三层需要三跳邻居。

如果平均每个节点有 \(d\) 个邻居，那么 \(K\) 层之后，依赖规模大约是：

\[
O(|B|d^K)
\]

这就是所谓的邻居爆炸。

更麻烦的是，训练时不仅要前向计算，还要反向传播。反向传播需要保存每一层参与计算的节点表示、边索引和中间激活。于是显存压力会迅速上升。

所以 GNN 在大图上的瓶颈，很多时候不是模型参数太多，而是参与计算的节点和边太多，导致激活内存太大。

## 第 14 页：解决大图训练内存问题的方法

解决这个问题的核心思路是：不要每次都展开完整 \(K\)-hop 邻域，而是用采样、分块或重计算来控制规模。

第一种方法是 Neighbor Sampling，代表方法是 GraphSAGE。它不是拿所有邻居，而是在每一层只采样固定数量的邻居。这样复杂度就从

\[
O(|B|d^K)
\]

变成近似：

\[
O(|B|\prod_{l=1}^{K}s_l)
\]

其中 \(s_l\) 是第 \(l\) 层采样的邻居数。

第二种是 layer-wise 或 subgraph sampling。它不是围绕每个节点完整展开邻域，而是在层或子图层面采样，减少参与反向传播的激活范围。

第三种是 Cluster-GCN。它先把大图划分成多个簇，每次训练一个或几个簇组成的子图。这样可以保留子图内部结构，同时控制 mini-batch 大小。

第四种是 GraphSAINT。它通过采样节点、边或随机游走得到子图，再用归一化修正采样偏差。

第五种是 gradient checkpointing。它的思路是前向传播时少保存一些中间激活，反向传播时再重新计算。这是用更多计算时间换更少显存。

这些方法的共同目标是：在尽量保留图结构信息的同时，控制消息传递和反向传播的内存成本。

## 第 15 页：总结

最后总结一下今天的内容。

第一，图是一种描述实体和关系的通用数据结构。很多真实世界数据，比如社交网络、分子、交通、推荐、引用网络，都天然适合用图表示。

第二，传统神经网络直接处理图会遇到困难，因为图大小不固定、邻居数量不固定、节点顺序没有意义，而且连接关系通常非常稀疏。

第三，GNN 的核心思想是 message passing。节点从邻居接收消息，聚合消息，再更新自己的表示。堆叠多层之后，节点就能吸收更远范围的结构信息。

第四，GNN 的聚合函数必须尊重图的排列不变性。无论节点编号怎么变，同一张图的预测结果都应该保持一致。

第五，大图训练的关键瓶颈是邻居爆炸和反向传播激活内存。解决方法包括邻居采样、子图采样、Cluster-GCN、GraphSAINT 和 gradient checkpointing。

一句话概括：GNN 是把神经网络放到图结构上，让信息沿着关系流动；而大图训练的难点，是如何在不丢失太多结构信息的前提下，把计算和显存控制住。

